# 🚀 Horror Maze - 9天开发计划（2人团队）

基于你们的游戏设计框架，重新制定开发计划。

---

## 📦 项目模块划分（8大模块）

### 模块1：迷宫系统 ⭐⭐⭐⭐⭐
**文件**：`Maze.h`, `Maze.cpp`
**优先级**：P0 核心
**工作量**：1天

**功能**：
```cpp
class Maze {
    std::vector<std::vector<int>> map;  // 地图数据
    sf::Vector2i playerStart;           // 玩家起点
    sf::Vector2i exitPos;               // 出口位置

    void loadFromFile(const std::string& filename);  // 加载固定地图
    bool isWall(int x, int y);
    int getCell(int x, int y);
};
```

**地图格式**（文本文件）：
```
20 20              # 宽度 高度
1 1 1 1 1 1 ...   # 0=空地 1=墙 2=出口 3=双胞胎 4=雪墙 5=打火机
1 0 0 0 0 1 ...
...
```

---

### 模块2：玩家系统 ⭐⭐⭐⭐⭐
**文件**：`Player.h`, `Player.cpp`
**优先级**：P0 核心
**工作量**：1.5天

**功能**：
```cpp
enum class MoveMode { WALK, RUN, CROUCH };

class Player {
    float x, y;                    // 位置
    float dirX, dirY;              // 方向向量
    float planeX, planeY;          // 相机平面向量
    MoveMode moveMode;

    float stamina;                 // 体力（可选P2）
    bool hasLighter;               // 是否有打火机
    bool lighterOn;                // 打火机是否开启
    float lighterFuel;

    void move(float deltaTime, const Maze& maze);
    void rotate(float angle);
    void switchMoveMode(MoveMode mode);
    void toggleLighter();
    float getSpeed() const;        // 根据moveMode返回速度
    float getSoundVolume() const;  // 根据moveMode返回音量
};
```

---

### 模块3：3D渲染引擎 ⭐⭐⭐⭐
**文件**：`Renderer.h`, `Renderer.cpp`
**优先级**：P0 核心
**工作量**：2天

**功能**：
```cpp
class Renderer {
    void renderFirstPerson(sf::RenderWindow& window,
                          const Player& player,
                          const Maze& maze,
                          const std::vector<Ghost>& ghosts);

    void renderTopDown(sf::RenderWindow& window,
                      const Player& player,
                      const Maze& maze);

    void applyDarkness(sf::RenderWindow& window, const Player& player);
    void renderLighting(sf::RenderWindow& window, const Player& player);

private:
    void raycasting(/* 参数 */);
};
```

**光线投射关键代码**：
- 参考：https://lodev.org/cgtutor/raycasting.html
- 每列投射一条光线
- 计算墙高并绘制垂直条

---

### 模块4：鬼的AI系统（行为树）⭐⭐⭐⭐
**文件**：`Ghost.h`, `Ghost.cpp`, `Pathfinding.h`, `Pathfinding.cpp`
**优先级**：P0 核心
**工作量**：2天

**功能**：
```cpp
enum class GhostState { PATROL, ALERT, CHASE };

class Ghost {
    float x, y;
    float speed;
    GhostState state;

    std::vector<sf::Vector2i> patrolPath;    // 巡逻路径
    std::vector<sf::Vector2i> chasePath;     // 追逐路径（A*）
    float alertTimer;                         // 警戒计时器
    float loseTrackTimer;                     // 丢失目标计时器

    void update(float deltaTime, const Player& player, const Maze& maze);
    void updatePatrol(float deltaTime);
    void updateAlert(float deltaTime, sf::Vector2f soundDir);
    void updateChase(float deltaTime, const Player& player, const Maze& maze);

    bool canSeePlayer(const Player& player, const Maze& maze);
    bool canHearPlayer(const Player& player);

    void transitionState(GhostState newState);
};

// 寻路模块
namespace Pathfinding {
    std::vector<sf::Vector2i> aStar(sf::Vector2i start,
                                     sf::Vector2i goal,
                                     const Maze& maze);
}
```

**状态转换逻辑**：
```
PATROL:
  if (canSee) -> CHASE
  if (canHear) -> ALERT

ALERT:
  if (canSee) -> CHASE
  if (timeout) -> PATROL

CHASE:
  if (catchPlayer) -> 玩家死亡
  if (loseTrack > 10s) -> ALERT
```

---

### 模块5：声音系统 ⭐⭐⭐⭐
**文件**：`SoundSystem.h`, `SoundSystem.cpp`
**优先级**：P1 重要
**工作量**：1.5天

**功能**：
```cpp
struct SoundSource {
    sf::Vector2f position;
    float volume;
    float maxRange;
    bool isLooping;
};

class SoundSystem {
    std::vector<SoundSource> activeSounds;

    void addSound(sf::Vector2f pos, float volume, float range);
    void update(float deltaTime);
    void clear();

    // 计算玩家听到的声音（立体声）
    sf::Vector2f calculateSoundDirection(const Player& player,
                                          const SoundSource& sound);

    // 鬼听到的最近声音
    sf::Vector2f getNearestSoundPosition(const Ghost& ghost, float hearingRange);
};
```

**声音可视化（P2可选）**：
```cpp
class SoundVisualizer {
    void renderSoundIndicators(sf::RenderWindow& window,
                               const Player& player,
                               const SoundSystem& soundSys);
    // 在屏幕边缘绘制方向指示器
};
```

**音频文件（使用SFML Audio）**：
- footstep_walk.wav
- footstep_run.wav
- ghost_footstep.wav
- twins_voice.wav
- ambient.wav

---

### 模块6："闪灵"系统 ⭐⭐⭐
**文件**：`VisionAbility.h`, `VisionAbility.cpp`
**优先级**：P1 重要
**工作量**：0.5天

**功能**：
```cpp
class VisionAbility {
    bool isActive;
    float cooldown;
    float currentCooldown;
    float duration;
    float currentDuration;
    std::vector<sf::Vector2i> pathToExit;

    void trigger(const Player& player, const Ghost& ghost, const Maze& maze);
    void update(float deltaTime);
    void render(sf::RenderWindow& window, const Player& player);
    bool canUse() const;
};
```

**触发条件检查**：
```cpp
float distToGhost = distance(player, ghost);
if (distToGhost < 3.0f && visionAbility.canUse()) {
    visionAbility.trigger(player, ghost, maze);
}
```

**渲染效果**：
- 屏幕闪白
- 在第一人称视角中绘制发光路径
- 淡出效果

---

### 模块7：环境交互系统 ⭐⭐⭐
**文件**：`Environment.h`, `Environment.cpp`
**优先级**：P1 重要
**工作量**：1天

**功能**：
```cpp
// 雪墙
class SnowWall {
    sf::Vector2i position;
    bool isIntact;
    bool playerInside;
    float durability;

    void enter(Player& player);
    void exit(Player& player);
    void damage(float amount);  // 鬼砸墙
    bool isDestroyed() const;
};

// 双胞胎
class Twins {
    sf::Vector2i position;
    bool hasTriggered;
    float stunDuration = 3.0f;

    bool checkTrigger(const Player& player);
    void trigger(Player& player);  // 硬控玩家
};

// 打火机道具
class LighterItem {
    sf::Vector2i position;
    bool isCollected;

    void collect(Player& player);
};
```

---

### 模块8：UI系统 ⭐⭐⭐
**文件**：`UI.h`, `UI.cpp`
**优先级**：P0 核心
**工作量**：1天

**功能**：
```cpp
class UI {
    sf::Font font;

    void drawMainMenu(sf::RenderWindow& window, int selectedOption);
    void drawHUD(sf::RenderWindow& window, const Player& player,
                const VisionAbility& vision);
    void drawGameOver(sf::RenderWindow& window, bool victory);

private:
    void drawLighterFuel(sf::RenderWindow& window, float fuel);
    void drawVisionCooldown(sf::RenderWindow& window, float cooldown);
    void drawSoundIndicators(sf::RenderWindow& window, /* ... */);
};
```

**HUD布局**：
```
┌────────────────────────────────────┐
│ 🔦 ████████░░ (打火机燃料)          │
│                                    │
│                      ← ○ → (声音)  │
│                                    │
│                                    │
│              [游戏画面]             │
│                                    │
│                                    │
│ ⚡ 闪灵冷却: 15s                    │
└────────────────────────────────────┘
```

---

## 📅 9天详细开发计划

### **第1天**（Day 1）- 基础框架

#### 人员A：迷宫 + 基础渲染
**任务**：
- [x] 创建 Maze.h/cpp
- [x] 实现地图加载（从文本文件）
- [x] 手工设计第一关地图（20×20）
- [x] 实现俯视图渲染（简单方块）

**交付物**：
- 能在俯视图中看到迷宫布局

#### 人员B：玩家基础系统
**任务**：
- [x] 创建 Player.h/cpp
- [x] 实现位置、方向属性
- [x] 实现基础移动（WASD键）
- [x] 实现碰撞检测（不能穿墙）

**交付物**：
- 玩家能在俯视图中移动

#### 晚上联调（30分钟）
- [x] 玩家在迷宫中移动
- [x] 测试碰撞检测

---

### **第2天**（Day 2）- 3D渲染基础

#### 人员A：光线投射引擎
**任务**：
- [x] 创建 Renderer.h/cpp
- [x] 实现光线投射算法（参考Lode教程）
- [x] 渲染墙体（简单颜色）
- [x] 测试第一人称视角

**难点**：
- 理解光线投射算法
- 建议：先运行Lode的示例代码，再移植到项目

**交付物**：
- 第一人称能看到迷宫墙体

#### 人员B：玩家进阶系统           //111
**任务**：
- [x] 实现3种移动模式（走/跑/蹲）
- [x] 实现模式切换（Shift/Ctrl）
- [x] 调整移动速度和手感
- [x] 实现旋转（鼠标或A/D键）

**交付物**：
- 移动流畅，3种模式可切换

#### 晚上联调（30分钟）
- [x] 第一人称移动测试
- [x] 调整速度参数

---

### **第3天**（Day 3）- AI基础

#### 人员A：优化渲染
**任务**：
- [x] 优化光线投射性能
- [x] 添加天空和地板渲染
- [x] 添加简单光照效果
- [x] 实现小地图（俯视图缩小版）

**交付物**：
- 渲染流畅（60 FPS）
- 画面更完整

#### 人员B：鬼的AI骨架                      // 111何为警戒
**任务**：
- [x] 创建 Ghost.h/cpp
- [x] 实现状态机框架（PATROL/ALERT/CHASE）           
- [x] 实现游走状态（沿预设路径）                          //细节商议 
- [x] 在地图中添加鬼的起点

**交付物**：
- 鬼能在迷宫中游走

#### 晚上联调（30分钟）
- [x] 在第一人称中看到鬼（简单形状）
- [x] 测试鬼的移动

---

### **第4天**（Day 4）- AI追踪

#### 人员A：A*寻路算法
**任务**：
- [x] 创建 Pathfinding.h/cpp
- [x] 实现A*算法（可参考现成代码）
- [x] 测试寻路（打印路径）
- [x] 优化性能（避免每帧计算）

**参考**：
- https://www.redblobgames.com/pathfinding/a-star/
- 可以使用AI生成基础代码

**交付物**：
- A*算法能正确计算路径

#### 人员B：鬼的追踪逻辑
**任务**：
- [x] 实现视觉检测（canSeePlayer）                       // 视觉？意为如果能直接看到，即使玩家没有动也会chase？
- [x] 实现追逐状态（CHASE）
- [x] 集成A*寻路
- [x] 实现抓住玩家判定

**交付物**：
- 鬼能看到并追踪玩家
- 抓到玩家后游戏结束

#### 晚上联调（1小时）
- [x] 完整测试追踪系统
- [x] 调整鬼的速度和视野范围
- [x] **里程碑**：核心玩法可玩！

---

### **第5天**（Day 5）- 声音系统                    //111

#### 人员A：声音系统框架
**任务**：
- [x] 创建 SoundSystem.h/cpp
- [x] 实现声音源管理
- [x] 实现距离衰减
- [x] 玩家移动时添加声音源

**交付物**：
- 玩家移动产生声音（数据层面）

#### 人员B：声音AI集成
**任务**：
- [x] 实现鬼的听觉检测（canHearPlayer）
- [x] 实现警戒状态（ALERT）
- [x] 状态转换完善（PATROL→ALERT→CHASE）
- [x] 测试声音追踪

**交付物**：
- 鬼能听到玩家的声音并警戒/追踪

#### 晚上联调（1小时）
- [x] 测试完整AI行为树
- [x] 调整参数（听觉范围、警戒时间等）

---

### **第6天**（Day 6）- 环境交互

#### 人员A：打火机 + 光照
**任务**：
- [x] 实现打火机道具（LighterItem）
- [x] 玩家拾取打火机
- [x] 实现打火机开关（F键）
- [x] 实现光照效果（照亮周围）
- [x] 实现燃料消耗                                 // 思考燃料消耗是否有必要

**难点**：
- 光照渲染（可以简化为圆形渐变）
- 鬼能看到打火机光源

**交付物**：
- 打火机系统完整

#### 人员B：雪墙 + 双胞胎                         // 这个我来？双胞胎贴图
**任务**：
- [x] 创建 Environment.h/cpp
- [x] 实现雪墙（SnowWall）
- [x] 实现进入/离开雪墙（E键）
- [x] 实现双胞胎（Twins）
- [x] 双胞胎硬控效果

**交付物**：
- 能躲进雪墙
- 碰到双胞胎会被硬控

#### 晚上联调（1小时）
- [x] 测试雪墙躲避鬼
- [x] 测试双胞胎陷阱
- [x] 测试打火机照明

---

### **第7天**（Day 7）- 特殊机制 + UI                       ///111

#### 人员A："闪灵"系统
**任务**：
- [x] 创建 VisionAbility.h/cpp
- [x] 实现触发条件检测
- [x] 计算到出口的路径（使用A*）
- [x] 实现渲染效果（闪光 + 路径）
- [x] 实现冷却计时

**交付物**：
- 闪灵系统完整

#### 人员B：UI系统
**任务**：
- [x] 创建 UI.h/cpp
- [x] 实现主菜单
- [x] 实现HUD（打火机燃料、闪灵冷却）
- [x] 实现游戏结束界面
- [x] 加载字体资源

**交付物**：
- UI完整且美观

#### 晚上联调（1小时）
- [x] 整合所有系统到 Game.cpp
- [x] 实现胜利条件（到达出口）
- [x] 实现失败条件（被抓）
- [x] 实现重新开始功能
- [x] **里程碑**：游戏完整可玩！

---

### **第8天**（Day 8）- 优化 + 测试

#### 上午（两人协作）：Bug修复
**任务**：
- [x] 全面测试游戏
- [x] 记录所有Bug
- [x] 修复崩溃和严重Bug
- [x] 测试边缘情况

**测试清单**：
```
□ 玩家能正常移动（3种模式）
□ 碰撞检测正确
□ 鬼的AI正常（游走、警戒、追逐）
□ A*寻路不卡顿
□ 打火机能正常使用
□ 雪墙能躲藏
□ 双胞胎能触发
□ 闪灵能正常触发
□ 到达出口能胜利
□ 被抓能显示失败
□ 重新开始功能正常
```

#### 下午人员A：性能优化
**任务**：
- [x] 检查帧率（目标60 FPS）
- [x] 优化光线投射
- [x] 优化A*计算频率
- [x] 减少不必要的渲染

#### 下午人员B：体验优化
**任务**：
- [x] 调整游戏难度
  - 鬼的速度
  - 听觉/视觉范围
  - 闪灵冷却时间
- [x] 调整颜色和氛围
  - 暗色调
  - 恐怖感
- [x] 添加音效（如果有时间）

#### 晚上：多轮测试
- [x] 完整游戏流程测试（至少5次）
- [x] 确认没有游戏阻断性Bug
- [x] 调整难度到"有挑战但可完成"

---

### **第9天**（Day 9）- 文档 + 最终准备

#### 上午人员A：README文档
**任务**：
- [x] 撰写 README.md
  - 项目简介
  - 游戏玩法说明
  - 操作说明
  - 编译步骤
  - 运行方法
  - 项目结构
  - 技术实现（光线投射、A*、行为树）
  - 开发日志
- [x] 代码注释检查

#### 上午人员B：最后Bug修复
**任务**：
- [x] 最后一轮测试
- [x] 修复发现的新Bug
- [x] 确保程序稳定

#### 下午（两人协作）：准备演示
**任务**：
- [x] 录制演示视频（可选）
- [x] 截图（菜单、游戏画面、胜利界面）
- [x] 准备答辩PPT（如需要）
- [x] 检查提交文件清单：
  ```
  □ 源代码（所有.h和.cpp文件）
  □ Visual Studio项目文件（.sln, .vcxproj）
  □ 资源文件（字体、地图文件、音效）
  □ README.md
  □ GAME_DESIGN.md
  □ 编译好的.exe文件
  ```

#### 傍晚：最终检查
- [x] 在另一台电脑上测试编译和运行
- [x] 确认所有依赖库（SFML DLL）都包含
- [x] 准备演示话术

---

## 🚨 风险管理与应对

### 高风险项

#### 1. 光线投射算法复杂
**风险等级**：⭐⭐⭐⭐
**缓解措施**：
- 第2天如果实现困难，使用现成代码
- Lode教程有完整C++代码，可以直接移植
- 最坏情况：只用俯视图（仍符合要求）

#### 2. A*算法难调试
**风险等级**：⭐⭐⭐
**缓解措施**：
- 使用在线A*可视化工具先理解
- 可以让AI生成代码
- 最坏情况：简化为直线追踪（不走最优路径）

#### 3. 行为树状态转换Bug
**风险等级**：⭐⭐⭐
**缓解措施**：
- 状态转换添加详细日志
- 每个状态独立测试
- 使用简化版行为树（就是状态机）

#### 4. 时间不足
**风险等级**：⭐⭐⭐⭐
**缓解措施**：
- 优先实现P0功能
- P1功能有选择地实现
- P2功能可以舍弃

### 功能优先级

**如果时间紧张，按此顺序删减：**

| 优先级 | 功能 | 可否删减 |
|--------|------|----------|
| P0 | 基础移动 + 迷宫 + 第一人称 | ❌ 不可删减 |
| P0 | 鬼的基础追踪（直线追踪） | ❌ 不可删减 |
| P0 | 胜利/失败条件 | ❌ 不可删减 |
| P1 | A*寻路 | ⚠️ 可简化为直线 |
| P1 | 声音系统 | ⚠️ 可简化为"鬼总能听到玩家" |
| P1 | 3种移动模式 | ⚠️ 可简化为2种（走/跑） |
| P1 | 打火机 | ⚠️ 可删减，改为"全场黑暗" |
| P1 | 雪墙 | ⚠️ 可删减 |
| P1 | 双胞胎 | ⚠️ 可删减 |
| P1 | 闪灵系统 | ⚠️ 可删减 |
| P2 | 声音可视化 | ✅ 可删减 |
| P2 | 体力系统 | ✅ 可删减 |
| P2 | 音效 | ✅ 可删减 |

---

## 💡 开发建议

### 使用AI的策略
1. **算法实现**：
   - 让AI生成A*算法代码
   - 让AI生成光线投射代码
   - 但必须自己理解和测试

2. **Bug修复**：
   - 遇到编译错误，直接问AI
   - 遇到逻辑Bug，描述现象让AI分析

3. **优化**：
   - 性能问题可以让AI提建议

### 参考开源代码
1. **光线投射**：
   - Lode's Raycasting Tutorial（强烈推荐）
   - GitHub: "raycasting engine C++"

2. **A*算法**：
   - Red Blob Games教程
   - GitHub: "A* pathfinding C++"

3. **SFML示例**：
   - SFML官方示例
   - GitHub: "SFML game C++"

### 团队协作
1. **每日同步**：
   - 早上10分钟：确认今天任务
   - 晚上30-60分钟：联调代码
   - 使用微信/QQ随时沟通

2. **代码管理**：
   - 建议使用Git（GitHub/Gitee）
   - 每人负责各自模块，减少冲突
   - 联调前先pull最新代码

3. **分工原则**：
   - 人员A：偏向渲染和算法
   - 人员B：偏向游戏逻辑和AI
   - 互相Review代码

---

## 📁 推荐项目结构

```
HorrorMaze/
├── HorrorMaze/                      # 源代码目录
│   ├── main.cpp                     # 入口
│   ├── Game.h / Game.cpp            # 游戏主类
│   ├── Maze.h / Maze.cpp            # 迷宫系统
│   ├── Player.h / Player.cpp        # 玩家系统
│   ├── Ghost.h / Ghost.cpp          # 鬼的AI
│   ├── Pathfinding.h / Pathfinding.cpp  # A*寻路
│   ├── Renderer.h / Renderer.cpp    # 3D渲染
│   ├── SoundSystem.h / SoundSystem.cpp  # 声音系统
│   ├── VisionAbility.h / VisionAbility.cpp  # 闪灵
│   ├── Environment.h / Environment.cpp  # 环境交互
│   ├── UI.h / UI.cpp                # 界面
│   └── Utils.h / Utils.cpp          # 工具函数
│
├── assets/                          # 资源文件
│   ├── fonts/
│   │   └── arial.ttf                # 字体文件
│   ├── maps/
│   │   └── level1.txt               # 第一关地图
│   └── sounds/                      # 音效（可选）
│       ├── footstep_walk.wav
│       ├── footstep_run.wav
│       └── twins_voice.wav
│
├── x64/Debug/                       # 编译输出
│   ├── HorrorMaze.exe
│   └── *.dll                        # SFML库
│
├── README.md                        # 项目文档
├── GAME_DESIGN.md                   # 游戏设计文档（本文档）
└── HorrorMaze.sln                   # VS解决方案

```

---

## ✅ 每日检查清单

### 每天晚上（联调前）
- [ ] 代码能编译通过
- [ ] 提交代码到Git（如使用）
- [ ] 记录今天完成的功能
- [ ] 记录遇到的问题
- [ ] 明天的任务清单

### 每天早上
- [ ] 拉取最新代码
- [ ] 回顾昨天进度
- [ ] 确认今天目标
- [ ] 预估完成时间

---

## 🎯 里程碑

### 里程碑1：第2天晚上
- ✅ 玩家能在第一人称中移动
- ✅ 能看到迷宫墙体

### 里程碑2：第4天晚上
- ✅ 鬼能追踪并抓住玩家
- ✅ **核心玩法可玩**

### 里程碑3：第7天晚上
- ✅ 所有系统整合完成
- ✅ **游戏完整可玩**

### 里程碑4：第9天下午
- ✅ 文档完成
- ✅ 准备演示
- ✅ **项目完成**

---

## 📊 工作量分配

| 模块 | 工作量 | 负责人 | 时间 |
|------|--------|--------|------|
| 迷宫系统 | 1天 | 人员A | Day 1 |
| 玩家系统 | 1.5天 | 人员B | Day 1-2 |
| 3D渲染 | 2天 | 人员A | Day 2-3 |
| 鬼的AI | 2天 | 人员B | Day 3-4 |
| A*寻路 | 1天 | 人员A | Day 4 |
| 声音系统 | 1.5天 | 两人 | Day 5 |
| 环境交互 | 1天 | 两人 | Day 6 |
| 特殊机制 | 1天 | 两人 | Day 7 |
| 优化测试 | 1天 | 两人 | Day 8 |
| 文档 | 0.5天 | 两人 | Day 9 |

**总工作量**：约18人·天
**实际人力**：2人 × 9天 = 18人·天
**结论**：时间刚好够，需要高效执行

---

## 🎓 学习资源

### 光线投射
- **Lode's Raycasting Tutorial**（必读）
  https://lodev.org/cgtutor/raycasting.html
- YouTube: "Raycasting Explained"

### A*算法
- **Red Blob Games**（必读）
  https://www.redblobgames.com/pathfinding/a-star/
- Visualizations帮助理解

### SFML
- 官方教程: https://www.sfml-dev.org/learn.php
- SFML 3.0 文档: https://www.sfml-dev.org/documentation/3.0.0/

### 行为树
- 简化版：就是状态机 + 条件判断
- 不需要完整的行为树框架

---

## 🏆 成功建议

1. **第一原则：让游戏能跑起来**
   - 不要追求完美，先实现再优化
   - 每天都有可运行的版本

2. **勤沟通**
   - 遇到问题立刻讨论
   - 不要一个人卡住浪费时间

3. **大胆用AI**
   - 算法代码可以让AI写
   - 但必须自己理解
   - 测试是自己的责任

4. **参考但不抄袭**
   - 学习开源代码的思路
   - 用自己的方式实现
   - 理解每一行代码

5. **留缓冲时间**
   - 前7天实现功能
   - 第8天专门处理意外
   - 第9天文档和演示

6. **测试驱动**
   - 每完成一个功能立刻测试
   - 不要等到最后集成才发现问题

7. **保持简约**
   - 电影感 = 简约
   - 不需要复杂的美术资源
   - 用颜色和光影营造氛围

---

**最后更新**：2024-12-21
**状态**：开发计划确认 ✅
**祝你们开发顺利！** 🚀

